on:
  pull_request:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
permissions:
  contents: read

jobs:
  build-docker-images:
    name: Build Docker images
    env:
      DOCKERHUB_SECRET: ${{ secrets.DOCKERHUB_TOKEN }}
    # NOTE: Secrets are not available for external contributors, so we skip the job.
    #       The left-part of the OR in the boolean condition covers non-pull-request
    #       events, the right-part covers pull requests with token, not a fork, not
    #       dependabot.
    #
    # `github.event_name != 'pull_request'` => runs for workflow_dispatch, push, workflow_run
    # `github.event.pull_request.head.repo.full_name == github.repository` => not a fork
    # `secrets.DOCKERHUB_TOKEN` => we have a DockerHub token
    # `github.actor != 'dependabot[bot]'` => ignore the Dependabot pull requests
    if: github.event_name != 'pull_request' ||
        (github.event.pull_request.head.repo.full_name == github.repository &&
        github.actor != 'dependabot[bot]')
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: 'autosubmit'  # org name
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: docker
          # TODO: for a separate issue, try pushing it
          push: false
          load: true
          tags: autosubmit/autosubmit:latest

      - name: Test the image
        run: |
          docker run --rm autosubmit/autosubmit:latest autosubmit --version
          # https://github.com/BSC-ES/autosubmit/issues/2359
          docker run --rm autosubmit/autosubmit:latest /bin/bash -c '[ -n $USER ] || exit 1'
